# Copy to .env and adjust. Do not commit .env.
PUID=1000
PGID=1000
TZ=Etc/UTC
CSRF_TRUSTED_ORIGINS=http://127.0.0.1:8000,http://localhost:8000
BB_HOST_PORT=8000
# Dedicated host directory for SQLite + app config (bind-mounted to /config)
BB_CONFIG_DIR=./data
